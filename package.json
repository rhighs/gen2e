{
  "name": "gen2e",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "publish": "lerna run build && lerna publish",
    "start": "tsx ./packages/gen2e-cli/src/index.ts",
    "test": "lerna run test",
    "format": "lerna run format"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^30.0.6",
    "lerna": "^8.1.3"
  }
}
